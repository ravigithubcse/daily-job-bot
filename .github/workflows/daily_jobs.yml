name: Daily IT Job Digest — Roy

on:
  schedule:
    - cron: '30 2 * * *'   # 8:00 AM IST = 2:30 AM UTC every day
  workflow_dispatch:         # Allow manual trigger

jobs:
  run-job-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate Gmail secrets
        env:
          GMAIL_SENDER:       ${{ secrets.GMAIL_SENDER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: |
          echo "=== GMAIL SECRET VALIDATION ==="
          if [ -z "$GMAIL_SENDER" ]; then
            echo "❌ GMAIL_SENDER is NOT SET"
          else
            echo "✅ GMAIL_SENDER = $GMAIL_SENDER"
          fi
          if [ -z "$GMAIL_APP_PASSWORD" ]; then
            echo "❌ GMAIL_APP_PASSWORD is NOT SET — email WILL fail silently"
            echo "   Fix: GitHub repo → Settings → Secrets → Actions → GMAIL_APP_PASSWORD"
          else
            LEN=${#GMAIL_APP_PASSWORD}
            echo "✅ GMAIL_APP_PASSWORD is SET ($LEN chars)"
            if [ "$LEN" -ne 16 ]; then
              echo "⚠  WARNING: Gmail App Passwords are exactly 16 chars — got $LEN"
              echo "   Regenerate at: myaccount.google.com/apppasswords"
            fi
          fi

      - name: Run Job Bot Pipeline
        env:
          GMAIL_SENDER:       ${{ secrets.GMAIL_SENDER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          GITHUB_TOKEN:       ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🤖 Roy Job Bot v3.0 starting..."
          echo "📅 $(date '+%A %d %B %Y — %I:%M %p UTC')"
          echo "🎯 ATS-matched IT jobs | Bengaluru only | 0-2 YOE | 13 platforms"
          python scripts/run_all.py

      - name: Upload email preview
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: email-preview-${{ github.run_number }}
          path: email_preview.html
          retention-days: 7

      - name: Upload jobs JSON
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jobs-found-${{ github.run_number }}
          path: jobs_found.json
          retention-days: 3
